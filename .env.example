# Required for /admin
ADMIN_PASSWORD=change-me

# Public site URL (no trailing slash)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: Polar Payment Link (overrides src/config.ts paymentLink)
PAYMENT_LINK=

# Optional: set automatically when you attach a Vercel Blob store
# BLOB_READ_WRITE_TOKEN=

# Optional / unused — do not block launch on this
# STRIPE_SECRET_KEY=
